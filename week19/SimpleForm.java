import javax.swing.*;

public class SimpleForm {
    public static void main(String[] args) {
        JFrame frame = new JFrame("Login Form");
        JLabel userLabel = new JLabel("Username:");
        title.Panel.add(titleLabel);
        sidePanel.setLayout(new BoxLayout(sidePanel,Box Layout.Y_AXIS);
        String[] buttons=("Add","Layout", " Delete","View");
         
        

       
}
}